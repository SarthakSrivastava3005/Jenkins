#!/bin/bash
echo "Hello from Jenkins build job with WebHook configured!"
echo "Now, whenever you push a commit to GitHub, Jenkins will automatically:
Receive webhook →
Pull latest code →
Trigger a build 🎯"
date
